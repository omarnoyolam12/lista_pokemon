import { Layout } from "@/components/layouts"

const Favoritos = () => {
    return (
        <Layout
            titulo="Favoritos"
        >
            <h1>Favoritos</h1>
        </Layout>
    )
}

export default Favoritos